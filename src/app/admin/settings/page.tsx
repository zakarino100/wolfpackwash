import { PageTemplate } from "@/components/ui/page-template";

export default function GenericPage() {
  return (
    <PageTemplate
      eyebrow="Wolf Pack Wash"
      title="Page scaffold in progress"
      description="This route is scaffolded and ready for service-specific copy, SEO, visuals, and conversion components in the next build phase."
    />
  );
}
